/*
* This is a design choice to eventually add full page customization via a configuration record in a database.
*
* */
scheduler.service('HomeModel', function() {
    this.getPageConfig = function(name, address, phone) {
        // todo: add AJAX to add a new client
    };
});